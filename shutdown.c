#include <stdio.h>

int main() {
    //  -t 60 means 60 seconds = 1 minutes, you can change it
    // Running this code means your computer is going to shutdown in 2 minutes.
system( "C:\\WINDOWS\\System32\\shutdown -s -t 120");
return 0;
}